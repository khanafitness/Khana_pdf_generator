{
  "name": "khana_pdf_generator",
  "version": "1.0.0",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "build": "npm install",
    "start": "npx @puppeteer/browsers install chrome@stable --path /opt/render/project/.chrome && node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer-core": "^21.3.8",
    "@puppeteer/browsers": "^2.0.0",
    "jszip": "^3.10.1"
  }
}
